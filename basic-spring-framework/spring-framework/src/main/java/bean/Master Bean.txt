Bean是什么? beanName(key) -> Bean对(value)
一个对象，被Spring管理的对象(A simple java Object)

TODO. 有哪些方式可以在Spring IoC中引入(Inject)Bean
Import Bean三种使用方式:
1. 直接注入bean到容器中，导入普通组件
   @Import(value = {MyBean.class}) ==> 如果引入的本身就是bean，则没有效果

2. 通过BeanDefinitionRegistrar来构建BeanDefinition再注册导入
   @Import(value = PersonBeanDefinitionRegister.class)

3. 使用Spring Boot自动装配, 通过完整路径来import
   AutoConfiguration + META-INF/spring.factories


TODO. 如何从Spring IoC容器中获取注入的Bean
1. @Autowired
2. Constructor 使用构造器可以不用添加注解
3. ApplicationContext getBean(), getBeanByClass() 从Context中直接获取Bean