Spring事务
1. 隔离级别基于数据库的隔离级别
2. 底层的工作原理:
   基于AOP来实现，会为标注了@Transactional注解的类型(或者方法)创建代理
   Spring creates proxies for all the classes annotated with @Transactional, either on the class or on any of the methods

   只用Proxy代理类来执行对数据库的请求操作
   class DemoTransactionProxy extends DemoTransactionClass {
       DemoTransactionClass target;
       判断是否有@Transactional注解
          创建数据库连接connection
          connection.autocommit=false 方便回滚
       target.test();
          事务会统一提交，反之会回滚
   }